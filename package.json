{
  "name": "todo-mvc",
  "scripts": {
    "build": "node_modules/.bin/elm make src/Main.elm --output public/app.js --debug",
    "watch": "chokidar '**/*.elm' -c 'npm run build' --initial"
  },
  "devDependencies": {
    "chokidar-cli": "^1.2.1",
    "elm": "^0.19.0-bugfix2"
  }
}
